= Riddler's Plan

. Kobweb's disabled is a lie about state, not a fact. Whether a module is launchable is "does a mount answer" — server-side knowledge. Same fix as (1): the vocabulary says which modules are live.
. The —  blank and the isPar string sniff. The service already knows which column is par; the page re-derives it by lowercasing a header. That's the one place the page second-guesses the frame it claims to just render.
