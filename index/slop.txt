Suggested order; you write, I review:

1. Store: entities keyed by id (label, meta, child ids, folded), plus normalize(company).
2. Components: <Node id depth/> reads its own entity and a primitive folded, keys children by id, uses children.length > 0 &&, and renders collapsed and the twist glyph from state.
3. build and reset: build normalizes, calls setState, runs flushSync(render), and returns the committed element count. reset unmounts, recreates the root and resets the store.
4. Folding:
   - unfold(limit) and fold(limit) plan from the store and apply each chunk in one set() inside flushSync.
   - foldTeams and collapse are single set() calls.
   - The click handler sits on the node and toggles through the store.
5. Fixture header: note that folded .kids isn't mounted, and why.
6. Clean up: delete the old commented-out prototype and put noUnusedLocals and noUnusedParameters back to true.
7. Run the ladder on SMOKE, then BENCH:
   - The element counts should line up with what pure reports.
   - Unfold should return exactly the rows the store planned.
   - After a reveal, the mounted .node count should match the store.

A decision to make before step 3: how build counts elements. Pure counts every element in the fragment inside the clock. React's closest match is host.querySelectorAll('*').length after the commit, which also runs inside the clock unless you take it after stopping the timer.

Hand me each step when it's done and I'll review it.

_____

THIS IS NO LONGER RELEVANT.
Here for historic reasons.

Claude issues to self:

1. Two-render cycle for teams' collapsed state (react.tsx:121-127)
   - First render: closed = (depth === 2 && children.length > 0) from fallback
   - Second render: after nodeRef updates, closed = store.isCollapsed(nodeRef, depth)
   - Teams render open on first pass, then close on second pass — inconsistent visual/measurement behavior
2. useEffect dependency on children.length per node (react.tsx:127)
   - Runs the fold operation on every render where children count changes
   - With thousands of team nodes, this could trigger excessive store updates
   - Should only run once when nodeRef is first set, not on every children.length change
3. Folded Set grows unbounded in long sessions
   - fold() and unfold() spread the entire existing Set and add/remove refs (react.tsx:92-93)
   - After many fold/unfold cycles, stale DOM element references remain in memory
   - reset() only clears on build reset, not during interactive reveal operations
4. Unused depth parameter in Leaf component (react.tsx:159)
   - Function signature includes depth: number but never uses it
5. Click handler captures stale closed state (react.tsx:131-137)
   - onClick uses closed from render time
   - If store changes between render and click, handler acts on stale state
   - Mitigated by flushSync but still a capture issue
6. Asymmetry in state read patterns (react.tsx:119 vs 124-125)
   - Line 119 uses hook: const store = useRevealStore()
   - Lines 124-125 use API: useRevealStore.getState().fold()
   - Mixed patterns work but inconsistent
7. Non-deterministic element counting by design (react.tsx:106-108)
   - countedElement increments counter for each created element
   - Conditional rendering of collapsed team children makes counts vary
   - This measures React's conditional rendering cost, which may be intentional but differs from pure.ts
8. Race between planFor and toggleChunk dispatch (react.tsx:216-219)
   - Plans which elements to fold based on current DOM state
   - Dispatches action immediately after
   - Minimal risk but DOM could theoretically change between plan and action


____

✓ React reveal refactoring completed:

1. ✓ Zustand store created — tracks folded refs (Set<Element>)
2. ✓ Node component refactored — reads collapsed state from store, conditionally unmounts .kids
3. ✓ fold/unfold/foldTeams updated — call store actions via useRevealStore.getState()
4. ✓ DOM queries simplified — planFor/toggleChunk still query DOM for refs, but dispatch to store
5. ✓ Cleanup — removed setters WeakMap, onClick handlers call store directly

Architecture:
- Store tracks folded teams (folded: Set<Element>)
- Initial state: folded set empty (all children render)
- User click or reveal() toggles teams in/out of folded set
- Conditionally rendered: children.length && !closed ? kids : null
- This unmounts children when folded (vs pure.ts CSS hiding)

_____
I understand: we're measuring architectural cost — what React takes out of developer control. DOM queries bypass React's control. We need React managing the reveal state end-to-end.

My understanding of stages:

1. Define reveal state shape — track which teams are unfolded (Set of IDs? Array? Map to rows count?), stored where (React context? Module-level ref?)
2. Lift state into Node — instead of local closed state, derive it from reveal state + initial collapsed setting. Nodes become controlled by reveal logic.
3. Rewrite fold/unfold/foldTeams — they update reveal state instead of querying DOM. planFor operates on reveal state (which nodes exist + current reveal state), not DOM selectors.
4. Remove DOM-based logic — planFor, rowsOf, selectors become unnecessary. Everything flows through React state.
5. Clean setters WeakMap — possibly replace with reveal state mechanism, or keep it for onClick handlers only.

My questions:

1. Should reveal state live in a React context so fold/unfold/foldTeams can access it? (They're module exports, not components.)
For reveal state, Zustand is the current standard choice.

2. Should we track all nodes' refs in React, or just track which team IDs are unfolded and derive the rest?
Option A: Track refs (revised)
- Zustand stores Set of node refs
- fold/unfold iterate refs, call setState directly
- Pro: Simple, leverages existing setters pattern, refs don't change
- Con: Still need to populate ref set as tree renders, manage lifecycle
- Measurement: Explicit ref-based control

3. Do we keep rendering hidden children (current CSS approach) or should React not render them at all?
We follow a typical React developer mindset and we conditionally render.

____

  1. Finish fully minimal react.
  2. Clean up Harness
  3. Refactor Harness.
_____

:model — 1228 lines, 9 main files. Naming findings, ranked:

1. Rung is Level plus a stowaway. Its four entries take their labels from Level; the fifth, REVEAL, isn't a level at all — it's the reveal measurement row. Your ToDo is already on it. Report.rung: String then stringly-types the whole thing back, so the enum isn't even load-bearing on the wire.

2. /ws is a bare literal in four places, never in Contract. Harness.kt:229, kobweb/Harness.kt:94, Socket.kt:23, plus the Ktor route at Routing.kt:38 — which carries your ToDo: remap /ws twice. Every other wire word (ON_OPEN, PARAMETER_*, DOM_KEY_*) is a const; this one isn't.

3. DOM_KEY_CONTAINER = "div", DOM_KEY_BUTTON = "button", DOM_KEY_TABLE_ROW = "tr". DOM_KEY_ means element id everywhere else (status, runId, tree, connection). These three are tag names. Same prefix, different kind of thing.

4. Report.type = "report". Frame gets its discriminator from kotlinx and says so in its own comment; Report hand-carries one. DATA_KEY_REPORT exists but is referenced only from ReportTest.kt:41 — the production default is the literal "report".

5. HR.kt holds CorporateDivision, CorporateGroup, ProductTeam. Nothing in it is HR. Person is the file next door.

6. PerformanceComparisonCategoryRow. "Category" carries no meaning — there are no categories. PerformanceComparisonValues is a wrapper around one List<Double>.

7. Three names for one framework list. UI_FRAMEWORK_* pairs → UIFrameworkUnderProfiling(key,label) → FrameworkOption(key,label). The middle one is the only one with a verb-ish name; FrameworkOption is the same shape serialized. Also those five Pairs are by lazy on compile-time constants.

8. DATASET_KEY_FAIL = "boom". Enum entry is FAIL, wire key is boom.

9. Stranded / commented-out. TOTAL sits at the bottom of PerformanceComparisonTable.kt (used by Matrix.kt:30); //const val DOM_KEY_TABLE_DATA :39 and //const val PARAMETER_STATUS :59 are dead lines.

10. PARAMETER_EVENT_CHANNEL = "events" — only consumer is :kobweb, which you excluded from the cleanup. Flagging so it isn't mistaken for live contract.

____


⏺ Four distinct "row" concepts are tangled under one word across the codebase:

  A. Tree-node row — .row CSS class, a <div> wrapping twist/name/meta inside each rendered node.
  - pure.ts:96 newNestedElement(..., 'row', ...)
  - react.js:53,70 e('div', {className: 'row'}, ...)
    Same concept, duplicated verbatim between the two fixtures (can't share Kotlin across runtimes, but the class name and shape are identical by convention, not by any shared source). Fine as-is; flag if it ever drifts.

  B. Table rows (<tr>) — three separate tables, same HTML element type, no relation to A:
  - Ladder rows: harness/Harness.kt — tr[id^="level-"], read/written via fill(), rungOf(), ladderRows(). One per rung (division/group/team/person).
  - Reveal row: harness/Harness.kt:310 — the single #reveal <tr>, filled via fillReveal()/postReveal(), reusing the same .cell(at) extension and column layout as the ladder rows above (legitimate reuse, same table).
  - Matrix rows: control/StatusMatrix.kt:70 rowElement(row: MatrixRow, ...) — one <tr> per MatrixRow (model/status/PerformanceComparisonTable.kt:21), an aggregated report row, entirely different page/table (:control's status performanceComparisonTable, not the live fixture page).
    These three are legitimately distinct tables; nothing to merge, but "ladder row"/"reveal row"/"performanceComparisonTable row" would be honest names — right now all three just say "row"/"rows" in comments and variables.

  C. Row count, not a DOM element — the number of Person leaf records revealed:
  - RevealMeasurement.rows: Int (harness)
  - unfold(limit): number return value (pure.ts, react.js)
  - rowsOf(nodeElement) (pure.ts) / rowsOf(box) (react.js) — counts .kids children to produce this
    This is a data metric ("how many people got shown"), sharing the word "rows" with A and B by coincidence only. Worth renaming to something like revealedCount/peopleRevealed to stop reading as a DOM row.

  D. The "rows" container id — the element rows get appended into:
  - harness/Harness.kt:96 fixtureRows = fixtureElement("rows") — container for the B-ladder <tr>s
  - control/Page.kt:24 Page.rows — container for the B-performanceComparisonTable <tr>s
    Different pages, never actually collide at runtime, but both are the same role (row-container) and both are the hardcoded-string slop flagged last message. Candidate for a single Contract.DOM_KEY_ROWS constant reused by both.

  Proposed naming, if you want it:
  - Keep .row (A) as-is — it's a rung's line, not a table row.
  - Rename RevealMeasurement.rows / unfold's return / rowsOf (C) → revealed / revealedCount to stop colliding with B.
  - Leave the three <tr> tables (B) distinct but comment them as "ladder row" / "reveal row" / "performanceComparisonTable row" instead of bare "row".
  - Add Contract.DOM_KEY_ROWS = "rows" for D, used by both harness/Harness.kt:96 and control/Page.kt:24.

  Say the word and I'll make the edits — otherwise this is just the map.

<div class="kids"><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Mark Carter</span><span class="meta">Support Specialist        Phoenix, AZ       (298) 245-1653</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Ashley Kim</span><span class="meta">Database Administrator    Austin, TX        (396) 490-3306</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Ryan Flores</span><span class="meta">UX Designer               Seattle, WA       (494) 735-4959</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">James Parker</span><span class="meta">Product Manager           Denver, CO        (592) 980-6612</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Maria Jackson</span><span class="meta">Technical Writer          New York, NY      (691) 225-8265</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Samuel Gutierrez</span><span class="meta">Business Analyst          Miami, FL         (789) 470-9918</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Ryan Kelly</span><span class="meta">Database Administrator    Boston, MA        (887) 716-1571</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Emma Hughes</span><span class="meta">Product Manager           Raleigh, NC       (985) 961-3224</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Justin Johnson</span><span class="meta">Solutions Architect       Boston, MA        (284) 206-4877</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Gregory Turner</span><span class="meta">Recruiter                 Seattle, WA       (382) 451-6530</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Larry Howard</span><span class="meta">Software Engineer         Chicago, IL       (480) 696-8183</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Nancy Clark</span><span class="meta">Security Analyst          Portland, OR      (578) 941-9836</span></div></div><div class="node depth-3"><div class="row"><span class="twist">·</span><span class="name">Sharon Bailey</span><span class="meta">Product Manager           Chicago, IL       (677) 187-1489</span></div></div></div>